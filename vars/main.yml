---

# default user
default_user: rywillia

# hostname
hostname: laptop

# jetbrains toolbox version
jetbrains_toolbox: "1.16.6016"

# groups to create
custom_groups:
  - docker
  - libvirt

# services
services:
  - cups-browsed
  - docker
  - libvirtd
