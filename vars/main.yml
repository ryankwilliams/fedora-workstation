---

# default user
default_user: rywillia

# hostname
hostname: laptop

# jetbrains toolbox version
jetbrains_toolbox: "1.8.3678"

# groups to create
custom_groups:
  - docker
  - libvirt

# services
services:
  - cups-browsed
  - docker
  - libvirtd
