---

# default user
default_user: rywillia

# jetbrains toolbox (this version will change over time)
jetbrains_toolbox: '1.8.3678'

# hostname
hostname: laptop

# groups to create
custom_groups:
  - docker